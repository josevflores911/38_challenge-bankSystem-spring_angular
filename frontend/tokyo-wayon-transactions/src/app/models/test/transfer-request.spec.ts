import { TransferDetailRequest } from '../TransferDetailRequest';

describe('TransferRequest', () => {
  it('should create an instance', () => {
    expect(new TransferDetailRequest()).toBeTruthy();
  });
});
